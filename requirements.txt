# Developer-friendly environment bootstrap:
#   python3 -m venv .venv
#   . .venv/bin/activate
#   pip install -r requirements.txt
#
# This installs the package itself in editable mode and includes the
# dependencies needed for the test suite and ONNX evaluation path.

-e .
onnxruntime>=1.18
pytest>=8.0
